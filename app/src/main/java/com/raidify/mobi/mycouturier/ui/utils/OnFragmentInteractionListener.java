package com.raidify.mobi.mycouturier.ui.utils;

import android.net.Uri;
// This interface was created for Fragment navigation.
// An alternative method for navigation was eventually used. It may be removed when no longer in use
public interface OnFragmentInteractionListener {
 void onFragmentInteraction(Uri uri);
}
